<!DOCTYPE html>
<html lang="es">

<%- include('partials/head.ejs') %>

<body>
    <!-- HEADER -->
    <%- include('partials/header.ejs') %>

  <main class="main-register">
    <form class="form-register" action="/register" method="POST" enctype="multipart/form-data">
      <fieldset>
        <legend class="title-form" > CREAR CUENTA </legend>
        <p>Compra más rápido y lleva el control de tus pedidos, ¡en un solo lugar!</p>
        <div class="campo-info">
          <input type="text" name="nombre" value="<%= ( locals.old && old.nombre ) ? old.nombre : '' %>" />
          <label class="label-form" for="nombre">Ingresa tu nombre</label>
          <% if( locals.errors && errors.nombre ){ %>
            <p class="message-error"> <%= errors.nombre.msg %>!! </p>
          <% } %>
        </div>
        <div class="campo-info">
          <input type="text" name="apellido" value="<%= ( locals.old && old.apellido ) ? old.apellido : '' %>"/>
          <label class="label-form" for="apellido">Ingresa tu apellido</label>
          <% if( locals.errors && errors.apellido ){ %>
            <p class="message-error"> <%= errors.apellido.msg %>!! </p>
          <% } %>
        </div>
        <div class="campo-info">
          <input type="text" name="email" value="<%= ( locals.old && old.email ) ? old.email : ''  %>"/>
          <label class="label-form" for="email">Ingresa tu email</label>
          <% if( locals.errors && errors.email ){ %>
            <p class="message-error"> <%= errors.email.msg %>!! </p>
          <% } %>
        </div>
        <div class="campo-info">
          <input type="number" name="telefono" value="<%= ( locals.old && old.telefono ) ? old.telefono : ''  %>"/>
          <label class="label-form" for="telefono">Ingresa tu numero de teléfono</label>
          <% if( locals.errors && errors.telefono ){ %>
            <p class="message-error"> <%= errors.telefono.msg %>!! </p>
          <% } %>
        </div>
        <div class="campo-info">
          <label for="img" class="label-image">Subir Imagen</label>
          <% if( locals.errors && errors.img ){ %>
            <p class="message-error"> <%= errors.img.msg %>!! </p>
          <% } %>
          <input name="img" class="input-image" type="file" id="">
        </div>
        <div class="campo-info">
          <input type="password" name="password" value="<%= ( locals.old && old.password ) ? old.password : ''  %>"/>
          <label class="label-form" for="constraseña">Ingresa tu contraseña</label>
          <% if( locals.errors && errors.password ){ %>
            <p class="message-error"> <%= errors.password.msg %>!! </p>
          <% } %>
        </div>
        <div class="campo-info">
          <input type="password" name="passwordConfirmation"/>
          <label class="label-form" for="constraseña">Confirma tu Contraseña</label>
          <% if( locals.errors && errors.passwordConfirmation ){ %>
            <p class="message-error"> <%= errors.passwordConfirmation.msg %>!! </p>
          <% } %>
        </div>
        <input class="btn-create-user" type="submit" value="Crear Cuenta" />
        <p>¿Ya tenés una cuenta? <a href="#" class="link-login">Inicia sesión</a></p>
      </fieldset>
    </form>
  </main>

    <!-- FOOTER -->
    <%- include('partials/footer.ejs') %>
</body>

</html>