<!DOCTYPE html>
<html lang="es">

<%- include('partials/head.ejs') %>

<body>
    <!-- HEADER -->
    <%- include('partials/header.ejs') %>

  <main class="main-login">
    <form class="form-login" method="POST">
      <fieldset>
        <legend class="title-form"> INICIAR SESIÓN </legend>
        <!-- <h2 class="form-title">Iniciar Sesión</h2> -->
        <p>¡Bienvenido de nuevo! Inicie sesión en su cuenta a continuación!</p>
        <div class="campo-info">
          <input type="text" name="email" value="<%= ( locals.old && old.email ) ? old.email : ''  %>"/>
          <label for="email" class="label-form">Ingresa tu email</label>
          <% if( locals.errors && errors.email ){ %>
            <p> <%= errors.email.msg %> </p>
          <% } %>
        </div>

        <div class="campo-info">
          <input type="password" name="password" />
          <label for="constraseña" class="label-form">Ingresa tu constraseña</label>
          <% if( locals.errors && errors.password ){ %>
            <p> <%= errors.password.msg %> </p>
          <% } %>
        </div>
        <div class="div-checkbox">
          <input type="checkbox" id="remember" name="remember" class="checkbox-remember">
          <label for="remember" class="label-remember">Recordame</label>
        </div>
        <input class="btn-init-sesion" type="submit" value="Iniciar Sesión" />
        <p class="recuperar-contra"><a href="#">¿Olvidaste tu contraseña?</a></p>
        <p>¿No tenés cuenta aun? <a class="link-crear-cuenta" href="/register">Crear Cuenta</a></p>
      </fieldset>
    </form>
  </main>

    <!-- FOOTER -->
    <%- include('partials/footer.ejs') %>
</body>

</html>